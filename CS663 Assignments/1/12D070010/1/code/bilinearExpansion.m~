function enlargedImage = bilinearExpansion(imageName)

origImage = imread(imageName);
origImageSize = size(origImage);

enlargedImageS = 
enlargedImage = zeros(3*origImageSize(1)-2, 2*origImageSize(2)-1);

for i = 1:(3*origImageSize(1)-2)
    for j = 1:(2*origImageSize(2)-1)
        
    end
end